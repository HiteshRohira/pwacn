{
  "name": "pwacn",
  "version": "0.0.0",
  "type": "module",
  "bin": {
    "pwacn": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup --config tsup.config.ts && cp -R ../../registry dist/registry",
    "typecheck": "tsc --noEmit"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^24.3.1",
    "tsup": "^8.5.0",
    "typescript": "^5.9.2"
  }
}
